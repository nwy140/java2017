package com.SilentWolfStudios.interfaceClasses;

public interface Pet { // interface have methods that all all abstracts, abstracts methods have no body/codeblocks
	// all methods created here has to be implemented at classes implemmenting it
	
		abstract void beFriendly();
		abstract void play();
		
}
